import React from "react";
import { View, Text } from "react-native";

export default function Resultlmc(props){
    return(
        <View>

            <Text>{props.messageResultlmc}</Text>

            <Text>{props.Resultlmc}</Text>

        </View>
    );
}